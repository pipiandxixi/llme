# llme — 个人数字克隆系统

## 产品定位

llme 是一个轻量级的个人数字克隆系统，通过结构化的个人画像提示词 + 个人知识记忆库，结合先进大语言模型的能力，复现特定个人的认知方式、思维框架和决策逻辑。

**核心设计原则：**
- 不做 SFT / 不做算法调优，完全依托 prompt engineering + RAG 实现
- 聚焦认知和决策逻辑的复刻，不追求语气/措辞的模仿
- 所有数据（画像、知识库）以 Markdown + YAML 格式存储，便于人工 review 和 git 版本控制
- 画像和知识库分离，支持独立维护和更新

---

## 核心概念

### 个人画像（Profile）
描述一个人"是谁、怎么想"的结构化文档集合，分三层：

| 层次 | 内容 | 更新频率 |
|------|------|----------|
| 核心层（core） | 价值观、基本信念、性格特质 | 低（年级） |
| 认知层（cognition） | 思维框架、决策模式、已知偏差 | 中（季度） |
| 领域层（domains） | 各领域的判断和方法论 | 中（月度） |
| 情境层（context） | 当前关注、关系网络、外部环境 | 高（周度） |

### 知识记忆库（Knowledge Base）
记录个人观点、判断、决策过程的结构化条目集合。每条条目都有 YAML 元数据，支持按 domain、tag、时间过滤检索。

### 克隆对话（Clone Chat）
用户向克隆提问，系统自动组装个人画像 + 相关知识条目作为上下文，调用 LLM 生成符合该人认知特征的回答。

---

## 核心用户流程

### 1. 克隆对话（Query Flow）
```
用户提问
  → 系统解析问题 domain 和关键词
  → 组装上下文（画像固定层 + 条件加载域文件 + 相关知识条目）
  → 调用 LLM（Claude）
  → 返回克隆回答
```

### 2. 知识录入（Knowledge Entry）
```
用户输入原始内容（文章摘录、想法、对话片段）
  → 用户（或辅助脚本）提取结构化条目
  → 填写 YAML 元数据（type/domain/tags）
  → 存入 knowledge/entries/ 或 knowledge/decisions/
```

### 3. 画像更新（Profile Update，手动触发）
```
用户触发更新脚本
  → 读取近期新增知识条目
  → 调用 LLM 分析是否与现有画像有出入或新增内容
  → 生成画像更新建议（diff 形式）
  → 用户 review 后手动合并到 profile/
```

---

## 非目标（Out of Scope）

- 语气、说话风格、口头禅的模仿
- 实时数据接入（社交媒体、邮件等）
- 自动化画像更新（无需人工审核）
- 多用户 / 云端部署
- 移动端

---

## 评估标准

克隆效果的衡量方式：维护一批"参考 Q&A"——真实场景下该人的回答样本，定期用克隆跑这些问题，观察一致性和偏差程度。

---

## 版本规划

| 阶段 | 目标 |
|------|------|
| v0.1 | 画像脚手架 + 知识库格式 + 本地 CLI 对话 |
| v0.2 | 本地 Web UI（对话界面 + 知识条目管理）|
| v0.3 | 画像更新辅助脚本（LLM 驱动的 diff 建议）|
| v0.4 | 知识检索增强（向量检索，按需引入）|
